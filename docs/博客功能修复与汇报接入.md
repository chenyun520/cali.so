# 博客功能修复与汇报接入

## 本次改动

- 培训课程下新增“汇报”分类，使用深绿封面与双栏内容卡片，手机端堆叠展示。
- 接入用户提供的《半年度分享.html》，保留 11 页原始内容和分步动画。新增章节选择、上一页、下一页、全屏及返回培训入口。
- 将演示中的 Tailwind CDN 运行脚本替换成本地预编译样式。背景图片和字体仍采用源文件中的外部地址。
- 文章服务端直接读取 Redis 反应数量，客户端进入文章时主动刷新。点赞支持乐观更新、失败回退，切换文章使用独立缓存。GET 只读取，不再初始化写入，避免覆盖并发点赞。
- 项目通讯稿 7 张图片重新从手册提取图生成无损 WebP，并跳过二次压缩。正文截图提前加载，沿用原始分辨率。
- 导航恢复始终可见的登录、注册入口，新增公开认证页面与加载失败提示。

## 认证配置待完成

现有 Clerk 生产实例仍绑定旧域名 cherishbloom.top，认证子域没有 DNS 解析。恢复入口不能解决认证服务本身无法加载的问题。需确认迁移到 chenyunsheep.top 或恢复旧域名，再完成 Clerk 域名及 DNS 配置。未创建测试用户、未修改真实用户信息。

## 验证记录

- `pnpm exec tsc --noEmit --pretty false` 通过。
- 本次修改的 TS/TSX 文件 ESLint 通过；`git diff --check` 通过。
- 独立 Next 预览生产构建通过，包含项目、通讯稿、培训页及点赞回归用例；不等同于带全部线上服务配置的主项目构建。
- 浏览器回归：修复前首次加载显示 0/0/0/0；修复后无需点击显示测试 API 的 12/8/5/3；点击后为 13/8/5/3；切换文章显示 7/6/4/2；模拟提交 500 后回退至 7/6/4/2，并显示失败提示。
- 浏览器确认培训页汇报卡片、演示首屏及下一页按钮正常。测试 API 和预览外壳仅在临时目录，不进入部署。
